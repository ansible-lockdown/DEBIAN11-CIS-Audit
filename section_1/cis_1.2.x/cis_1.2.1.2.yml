---

{{ if .Vars.deb11cis_level_1 }}
  {{ if .Vars.run_heavy_tests }}
    {{ if .Vars.deb11cis_rule_1_2_1_2 }}
command:
  repos_configured:
    title: 1.2.1.2 | Ensure package manager repositories are configured | Manual
    exit-status: 0
    exec: echo "MANUAL - Ensure repos match site policy"
    stdout:
    - '!/MANUAL/'
    meta:
      server: 1
      workstation: 1
      CIS_ID:
      - 1.2.1.2
      NIST800-53R5: SI-2
    {{ end }}
  {{ end }}
{{ end }}
