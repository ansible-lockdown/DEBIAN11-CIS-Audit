---

{{ if .Vars.deb11cis_gui }}
  {{ if .Vars.deb11cis_level_1 }}
    {{ if .Vars.deb11cis_rule_1_7_10 }}
file:
  /etc/gdm/custom.conf:
    title: 1.7.10 | Ensure XDCMP is not enabled
    path: /etc/gdm/custom.conf
    exists: true
    contents:
    - '!/^Enable( |)=( |)true/'
    meta:
      server: 1
      workstation: 1
      CIS_ID:
      - 1.7.10
    {{ end }}
  {{ end }}
{{ end }}
