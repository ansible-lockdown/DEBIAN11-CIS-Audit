---

{{ if .Vars.deb11cis_level_1 }}
  {{ if .Vars.deb11cis_rule_4_1_6 }}
command:
  ufw_open_ports:
    title: 4.1.6 | Ensure ufw firewall rules exist for all open ports | Manual
    exec: echo "Manual - Please confirm firewall rules exist for open ports match site policies"
    exit-status: 0
    stdout:
    - '!/^Manual/'
    meta:
      server: 1
      workstation: 1
      CIS_ID:
      - 4.1.6
      NIST800-53R5:
      - SC-7
  {{ end }}
{{ end }}
