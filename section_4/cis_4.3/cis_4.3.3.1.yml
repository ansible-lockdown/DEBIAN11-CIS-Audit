---

{{ if .Vars.deb11cis_level_1 }}
  {{ if .Vars.deb11cis_rule_4_3_3_1 }}
command:
  ipt6_def_deny:
    title: 4.3.3.1 | Ensure ip6tables default deny firewall policy | Manual
    exec: echo "Manual - Please confirm ip6tables default denies are configured and matched site policy"
    exit-status: 0
    stdout:
    - '!/^Manual/'
    meta:
      server: 1
      workstation: 1
      CIS_ID:
      - 4.3.3.1
      NIST800-53R5:
      - CA-9
      - SC-7
  {{ end }}
{{ end }}
