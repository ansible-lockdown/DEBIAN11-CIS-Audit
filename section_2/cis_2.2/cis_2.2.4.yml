---

{{ if .Vars.deb11cis_level_1 }}
  {{ if not .Vars.deb11cis_telnet_client }}
    {{ if .Vars.deb11cis_rule_2_2_4 }}
package:
  telnet_client:
    title: 2.2.4 | Ensure telnet client is not installed
    installed: false
    name: telnet
    meta:
      server: 1
      workstation: 1
      CIS_ID:
      - 2.2.4
      NIST800-53R5:
      - CM-7
    {{ end }}
  {{ end }}
{{ end }}
