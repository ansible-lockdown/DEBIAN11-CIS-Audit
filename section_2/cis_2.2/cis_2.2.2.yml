---

{{ if .Vars.deb11cis_level_1 }}
  {{ if not .Vars.deb11cis_rsh_client }}
    {{ if .Vars.deb11cis_rule_2_2_2 }}
package:
  rsh:
    title: 2.2.2 | Ensure rsh client is not installed
    installed: false
    name: rsh
    meta:
      server: 1
      workstation: 1
      CIS_ID:
      - 2.2.2
      NIST800-53R5:
      - CM-7
    {{ end }}
  {{ end }}
{{ end }}
